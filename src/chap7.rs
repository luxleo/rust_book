// The main downside to the style that uses files named mod.rs is that your project
// can end up with many files named mod.rs, which can get confusing when you
// have them open in your editor at the same time.
pub mod human;
pub mod war;